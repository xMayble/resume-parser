import React from 'react';
import './Navbar.css';

function Navbar() {
    return (
        <nav className="navbar">
            <header className="navbar-header">Resume Parser</header>
        </nav>
    );
}

export default Navbar;